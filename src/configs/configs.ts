class Configs {// Aqui tem só os dados de configuração de send de email
    public host="smtp.gmail.com"; //endereço do servidor smtp do gmail
    public port = 465; // porta que o smtp usa
    public user = "site.generico04@gmail.com";// endereço de email a ser utilizado (nesse caso é um email criado só para isso)
    public password = "mzmmpbthiutwzrpn"; // senha de apps ( específica para esta utilização)
}

export default new Configs;